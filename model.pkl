import pickle
from google.colab import files

# Use the 'model' variable which contains your trained SVM
with open('model.pkl', 'wb') as f:
    pickle.dump(model, f)

# This downloads the actual file to your computer
files.download('model.pkl')
